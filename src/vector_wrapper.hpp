#ifndef VECTOR_WRAPPER_HPP
#define VECTOR_WRAPPER_HPP

template <class T>
class vector_wrapper{
    public:
        vector_wrapper();
        ~vector_wrapper();
        void push_back(T value);
        
}

#endif
